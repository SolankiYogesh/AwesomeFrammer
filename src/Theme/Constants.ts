const commonConstant = {
  appUser: {}
}

const asyncStorageKeys = {
  userData: 'userData',
  OTP: 'OTP',
  Token: 'Token',
  posts: 'POSTS',
  app_build: 'Update'
}

const MAXLENGTH = {
  PASSWORD: 30
}

const timerObject = {
  START: 'START',
  END: 'END',
  SLEEP: 'SLEEP',
  AWAKE: 'AWAKE'
}

export default {
  commonConstant,
  asyncStorageKeys,
  MAXLENGTH,
  timerObject
}
